<%= form_with(model: following) do |form| %>
  <% if following.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(following.errors.count, "error") %> prohibited this following from being saved:</h2>

      <ul>
        <% following.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :follower, style: "display: block" %>
    <%= form.text_field :follower %>
  </div>

  <div>
    <%= form.label :followed, style: "display: block" %>
    <%= form.text_field :followed %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
