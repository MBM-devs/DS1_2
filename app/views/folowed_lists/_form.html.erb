<%= form_with(model: folowed_list) do |form| %>
  <% if folowed_list.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(folowed_list.errors.count, "error") %> prohibited this folowed_list from being saved:</h2>

      <ul>
        <% folowed_list.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :list_id, style: "display: block" %>
    <%= form.text_field :list_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
